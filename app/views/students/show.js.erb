// alert('this response was called!');
// console.log('this is being called from the remote true call I\'ve created');
// An call to upload the next month for 
$('#event_calendar').html("<%= j (render partial: 'calendar', locals: {events: @events}) %>");

